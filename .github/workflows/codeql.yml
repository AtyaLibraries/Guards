name: CodeQL

on:
  push:
    branches: [development, master]
  pull_request:
    branches: [development, master]
  schedule:
    - cron: "23 3 * * 1"
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    uses: AtyaLibraries/github-workflows/.github/workflows/codeql-csharp.yml@f6190353bf05e780bd2b98a0980fe1a7b0e93a27 # v1
    with:
      solution: ./Guards.sln
